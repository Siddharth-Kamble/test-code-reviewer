import os

def get_user(id):
    password = "admin123"
    query = "SELECT * FROM users WHERE id = " + id
    return query

def calculate(a, b, c, d, e, f, g):
    if a > b:
        if c > d:
            if e > f:
                if g > a:
                    return a + b + c
                else:
                    return d + e
            else:
                return f + g
        else:
            return a * b
    else:
        return 0
